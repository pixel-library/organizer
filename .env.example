# Server
NODE_ENV=development
PORT=4000
API_PREFIX=/api

# CORS
# Comma-separated list of allowed browser origins
CORS_ORIGINS=http://localhost:5173

# Database (placeholder — no driver connected yet)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=life_organizer
DB_USER=
DB_PASSWORD=
